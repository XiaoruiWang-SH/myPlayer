# myPlayer 开发进度列表

| 项目 | 内容 |
| --- | --- |
| 文档版本 | v0.1 |
| 日期 | 2026-08-27 |
| 关联文档 | [PRD.md](./PRD.md) / [TECHNICAL_DESIGN.md](./TECHNICAL_DESIGN.md) |

> **执行原则：一步一验证。** 每个步骤必须完成其「验证」并落库提交后，才能开始下一步。验证不通过先修复，不把问题带入下一步。阶段切换前，该阶段所有步骤与验证项必须全部 ✅。

## 状态标记

`⬜ 未开始`　`🔄 进行中`　`✅ 已完成（标注日期与 commit）`

## 阶段总览

| 阶段 | 目标 | 对应需求 | 对应里程碑 | 状态 |
| --- | --- | --- | --- | --- |
| 0 项目基建 | 工程可运行、窗口可打开 | — | M1 前置 | ⬜ |
| 1 播放核心 | 单文件播放控制完整可用 | FR-01~06 | M1 | ⬜ |
| 2 播放列表与快捷键 | 多曲目播放 + 全键盘操作 | FR-07、FR-09~15、PRD §6 快捷键表 | M2 | ⬜ |
| 3 设置与记忆 | 步长设置、状态记忆、媒体键 | FR-16~24 | M3 | ⬜ |
| 4 打磨与发布 | 全量回归 + 打包发布 | PRD §8 全部验收项 | M4 | ⬜ |

---

## 阶段 0：项目基建

### 步骤

- [ ] **0.1** 初始化 git 仓库与 npm 项目；安装并锁定依赖版本：`electron`、`electron-vite`、`electron-builder`、`typescript`、`electron-store`、`vitest`
- [ ] **0.2** 构建配置：`electron.vite.config.ts`、tsconfig（main/preload/renderer 三套）、`electron-builder.yml`（appId、productName、mac category）、npm scripts（`dev` / `typecheck` / `build`）
- [ ] **0.3** 三进程骨架：主进程创建窗口（最小尺寸 480×360、拦截新窗口与导航，见技术文档 §5）、空 preload、renderer 占位页面；应用菜单雏形（保留 About/Quit/编辑）

### 验证

- [ ] `npm run dev` 正常打开窗口，控制台无错误
- [ ] `npm run typecheck` 通过
- [ ] 提交入库，commit message 注明「阶段 0 项目基建」

---

## 阶段 1：播放核心（M1，FR-01~06）

> 本阶段无播放列表，通过「打开单个文件」方式验证播放能力；⌘O 多选入列表在阶段 2 接入。

### 步骤

- [ ] **1.1** `src/shared/types.ts` 共享类型；主进程注册 `media://` 自定义协议（仅限显式加入的白名单路径，技术文档 §5）；`openFiles` IPC 通道（单选，阶段 2 改多选）
- [ ] **1.2** `Player` 类封装 `<audio>`：`load/play/pause/toggle`（FR-01）
- [ ] **1.3** 控制栏播放/暂停按钮，图标随播放状态同步（FR-01）
- [ ] **1.4** 进度条：当前位置/总时长 `mm:ss` 显示，支持点击与拖拽跳转；`timeupdate` 节流 ~250ms（FR-02）
- [ ] **1.5** `seekBy` 快进/快退，钳制到 `[0, duration]`；步长暂用默认值常量 5 秒，阶段 3 接入设置（FR-03）
- [ ] **1.6** 音量滑块（0–100%）+ 静音切换与视觉标识；音量加减逻辑封装为独立函数供阶段 2 快捷键复用（FR-04、FR-05）
- [ ] **1.7** 播放速度档位切换 0.5 / 0.75 / 1.0 / 1.25 / 1.5 / 2.0，默认 1.0（FR-06）
- [ ] **1.8** Vitest 单元测试：`seekBy` 边界钳制（负值、超时长、无时长）

### 验证

- [ ] 用样例 MP3（本地无素材时可用 ffmpeg 生成）验证：播放/暂停切换正常
- [ ] 进度条拖拽/点击跳转准确，时间显示格式 `mm:ss`
- [ ] 快进退在文件首尾不越界
- [ ] 音量滑块与静音行为符合 FR-04/05；速度各档位生效
- [ ] `npm run test`（Vitest）与 `npm run typecheck` 通过
- [ ] 提交入库，commit message 注明对应条目（如「实现 FR-01~06」）

---

## 阶段 2：播放列表与快捷键（M2，FR-07、FR-09~15、PRD §6）

### 步骤

- [ ] **2.1** `Playlist` 模型：`Track`/`PlaylistState` 结构；按路径去重（大小写不敏感），非 `.mp3` 拒绝并轻提示（FR-13、FR-14）
- [ ] **2.2** 列表 UI：条目显示文件名（去扩展名）与时长、当前曲目高亮、单击播放、空状态引导文案（FR-10、FR-12、FR-15）
- [ ] **2.3** 文件接入：⌘O 对话框改多选入列表；拖拽入窗口（`webUtils.getPathForFile`）；工具栏「添加/删除/清空」（FR-09、FR-10）
- [ ] **2.4** 上一首/下一首逻辑与边界（首尾行为按当前循环模式）；⌘←/⌘→ 对应
- [ ] **2.5** 播放结束行为 + 三档循环模式切换（列表循环 → 单曲循环 → 顺序播放，默认列表循环）（FR-07、FR-11）
- [ ] **2.6** 时长异步串行探测（隐藏 `<audio preload="metadata">`），失败标记不可播放（FR-12）
- [ ] **2.7** 快捷键管理器：PRD §6 完整映射表（空格/方向键/⌘方向键/空格与 M、L/⌘O/⌘,）、命中 `preventDefault`、输入框焦点豁免规则
- [ ] **2.8** 播放错误处理：损坏/无法解码 → 提示并自动跳下一首（FR-08）
- [ ] **2.9** Vitest 单元测试：循环模式下一首计算、快捷键组合解析、扩展名过滤与去重

### 验证（对照 PRD §8 验收标准逐项）

- [ ] 拖入多个 MP3 全部入列表并可连续播放（验收 1）
- [ ] ⌘←/⌘→ 切歌；三种循环模式行为正确（验收 4）
- [ ] 空格暂停/恢复在列表场景下仍正确（验收 2 复测）
- [ ] 拖入非 MP3 有轻提示且不崩溃（验收 8 前半）
- [ ] 全部快捷键逐一手动过一遍，含输入框焦点场景
- [ ] `npm run test` 与 `npm run typecheck` 通过
- [ ] 提交入库，commit message 注明对应条目

---

## 阶段 3：设置与记忆（M3，FR-16~24）

### 步骤

- [ ] **3.1** `electron-store` 接入主进程，按技术文档 §3.4 schema 初始化默认值（`seekStep: 5` 等）
- [ ] **3.2** 设置弹窗：菜单「设置…」+ ⌘, 打开；步长输入校验 1–120 秒，修改立即生效并持久化；替换阶段 1 的步长常量（FR-16、FR-17、FR-18）
- [ ] **3.3** 状态落盘：退出前完整快照、播放中约每 5 秒防抖落盘进度、设置/音量/静音/速度/循环变更即时落盘（FR-19、FR-22）
- [ ] **3.4** 启动恢复：加载快照 → 主进程 `filterExisting` 校验文件存在性 → 失效条目移除并轻提示 → 恢复列表/进度/音量/速度/循环，**保持暂停态**（FR-19、FR-20、FR-21）
- [ ] **3.5** MediaSession 接入：曲目元数据、play/pause/previoustrack/nexttrack/seekto handlers、`setPositionState` 节流更新（FR-23、FR-24）
- [ ] **3.6** 媒体键真机实测（键盘 + 耳机线控、应用失焦/最小化场景）；不稳定则切换 `globalShortcut` 兜底方案并记录结论（技术文档 §3.5）
- [ ] **3.7** Vitest 单元测试：设置校验（越界值、非数字拒绝）

### 验证（对照 PRD §8 验收标准逐项）

- [ ] 步长改 30 秒后 ←/→ 立即生效，重启后仍生效（验收 5）
- [ ] 播放中途退出 → 重新打开：列表、曲目、进度（误差 ≤ 5 秒）、音量、速度、循环全部恢复，且为暂停态（验收 6）
- [ ] 强制杀掉进程（模拟崩溃）后重启，进度大致恢复（误差 ≤ 5 秒）
- [ ] 恢复时把某文件移走再启动：该条被移除并有提示，其余正常（验收 6 延伸）
- [ ] 应用最小化/失焦时媒体键可控；控制中心「正在播放」显示曲目标题（验收 7）
- [ ] `npm run test` 与 `npm run typecheck` 通过
- [ ] 提交入库，commit message 注明对应条目

---

## 阶段 4：打磨与发布（M4）

### 步骤

- [ ] **4.1** 错误路径全量回归：非 MP3、损坏文件、加载失败、时长探测失败的提示与降级行为统一（FR-08、FR-14、FR-21）
- [ ] **4.2** 深色/浅色模式适配验证（跟随系统）
- [ ] **4.3** 窗口布局回归：最小尺寸 480×360、拉大后布局不破、工具栏/列表/控制栏比例合理
- [ ] **4.4** Toast 提示一致性与自动消失行为复查（PRD §5.3）
- [ ] **4.5** `electron-builder` 打包 dmg + zip（arm64 + x64），安装后启动验证
- [ ] **4.6** 按 PRD §8 验收标准全量回归，逐项勾选
- [ ] **4.7** 文档自检（AGENTS.md §3 清单）：PRD/技术文档/README 与实现双向一致；README「当前状态」更新为已发布状态

### 验证

- [ ] PRD §8 验收标准 8 项全部 ✅
- [ ] 打包产物在本机安装、启动、播放、退出恢复全流程通过
- [ ] 文档自检清单逐项通过
- [ ] 打 tag（v1.0）并归档

---

## 附：需求 → 阶段对照表

| 需求条目 | 阶段 |
| --- | --- |
| FR-01~06 | 阶段 1 |
| FR-07 | 阶段 2（依赖循环模式） |
| FR-08 | 阶段 2 实现，阶段 4 回归 |
| FR-09~15 | 阶段 2 |
| FR-16~18 | 阶段 3 |
| FR-19~22 | 阶段 3 |
| FR-23~24 | 阶段 3 |
| PRD §6 快捷键表 | 阶段 2（音量/步长的数值逻辑阶段 1 已备好） |
| PRD §7 非功能需求 | 阶段 4 统一验证（性能、兼容性随阶段验证覆盖） |
