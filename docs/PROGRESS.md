# myPlayer 开发进度列表

| 项目 | 内容 |
| --- | --- |
| 文档版本 | v0.5（新增阶段 8（v1.3.1 补丁）规划，2026-08-28 评审通过） |
| 日期 | 2026-08-28 |
| 关联文档 | [PRD.md](./PRD.md) / [TECHNICAL_DESIGN.md](./TECHNICAL_DESIGN.md) |

> **执行原则：一步一验证。** 每个步骤必须完成其「验证」并落库提交后，才能开始下一步。验证不通过先修复，不把问题带入下一步。阶段切换前，该阶段所有步骤与验证项必须全部 ✅。

## 状态标记

`⬜ 未开始`　`🔄 进行中`　`✅ 已完成（标注日期与 commit）`

## 阶段总览

| 阶段 | 目标 | 对应需求 | 对应里程碑 | 状态 |
| --- | --- | --- | --- | --- |
| 0 项目基建 | 工程可运行、窗口可打开 | — | M1 前置 | ✅ 2026-08-27 |
| 1 播放核心 | 单文件播放控制完整可用 | FR-01~06 | M1 | ✅ 2026-08-27 |
| 2 播放列表与快捷键 | 多曲目播放 + 全键盘操作 | FR-07、FR-09~15、PRD §6 快捷键表 | M2 | ✅ 2026-08-27 |
| 3 设置与记忆 | 步长设置、状态记忆、媒体键 | FR-16~24 | M3 | ✅ 2026-08-27 |
| 4 打磨与发布 | 全量回归 + 打包发布 | PRD §8 全部验收项 | M4 | ✅ 2026-08-27 |
| 5 转录文稿（v1.1） | 左右分栏 + Deepgram 自动转录 + 同步文稿 | FR-25~31 | M5 | ✅ 2026-08-28（发布 e69ee3f，tag v1.1） |
| 6 媒体库与列表状态（v1.2） | 专属库导入/重命名/删除、每首进度与已播放状态 | FR-32~36 | M6 | ✅ 2026-08-28（发布 61b34a4，tag v1.2） |
| 7 交互改进（v1.3） | 手动播放、条目右键菜单、清空二次确认、文稿选择 | FR-37~40 | M7 | ✅ 2026-08-28（发布 f78f65b，tag v1.3） |
| 8 v1.3.1 补丁 | 重新转录二次确认、打包前清理旧产物 | FR-41、FR-42 | M8 | 🔄 进行中 |

---

## 阶段 0：项目基建

### 步骤

- [x] **0.1** 初始化 git 仓库与 npm 项目；安装并锁定依赖版本：`electron`、`electron-vite`、`electron-builder`、`typescript`、`electron-store`、`vitest`
- [x] **0.2** 构建配置：`electron.vite.config.ts`、tsconfig（main/preload/renderer 三套）、`electron-builder.yml`（appId、productName、mac category）、npm scripts（`dev` / `typecheck` / `build`）
- [x] **0.3** 三进程骨架：主进程创建窗口（最小尺寸 480×360、拦截新窗口与导航，见技术文档 §5）、空 preload、renderer 占位页面；应用菜单雏形（保留 About/Quit/编辑）

### 验证

- [x] `npm run dev` 正常打开窗口，控制台无错误（2026-08-27 用户确认窗口显示正常）
- [x] `npm run typecheck` 通过
- [x] 提交入库，commit message 注明「阶段 0 项目基建」

---

## 阶段 1：播放核心（M1，FR-01~06）

> 本阶段无播放列表，通过「打开单个文件」方式验证播放能力；⌘O 多选入列表在阶段 2 接入。

### 步骤

- [x] **1.1** `src/shared/types.ts` 共享类型；主进程注册 `media://` 自定义协议（仅限显式加入的白名单路径，技术文档 §5）；`openFiles` IPC 通道（单选，阶段 2 改多选）
- [x] **1.2** `Player` 类封装 `<audio>`：`load/play/pause/toggle`（FR-01）
- [x] **1.3** 控制栏播放/暂停按钮，图标随播放状态同步（FR-01）
- [x] **1.4** 进度条：当前位置/总时长 `mm:ss` 显示，支持点击与拖拽跳转；`timeupdate` 节流 ~250ms（FR-02）
- [x] **1.5** `seekBy` 快进/快退，钳制到 `[0, duration]`；步长暂用默认值常量 5 秒，阶段 3 接入设置（FR-03）
- [x] **1.6** 音量滑块（0–100%）+ 静音切换与视觉标识；音量加减逻辑封装为独立函数供阶段 2 快捷键复用（FR-04、FR-05）
- [x] **1.7** 播放速度档位切换 0.5 / 0.75 / 1.0 / 1.25 / 1.5 / 2.0，默认 1.0（FR-06）
- [x] **1.8** Vitest 单元测试：`seekBy` 边界钳制（负值、超时长、无时长）

### 验证

- [x] 用样例 MP3（本地无素材时可用 ffmpeg 生成）验证：播放/暂停切换正常（2026-08-27 用户确认）
- [x] 进度条拖拽/点击跳转准确，时间显示格式 `mm:ss`（2026-08-27 用户确认；自动测试中 seek 到 100s/300s 均准确）
- [x] 快进退在文件首尾不越界（`clampSeekTime` 单元测试覆盖：负值、超时长、无时长）
- [x] 音量滑块与静音行为符合 FR-04/05；速度各档位生效（2026-08-27 用户确认）
- [x] `npm run test`（Vitest）与 `npm run typecheck` 通过（8 tests passed，2026-08-27）
- [x] 提交入库，commit message 注明对应条目（如「实现 FR-01~06」）

> 过程记录：期间修复「播放约 1 秒中断」问题——根因是 `media://` scheme 权限缺 `standard: true` 导致媒体管道重发请求后中断，且需显式 206 + `Content-Range` 才能使 `seekable` 生效；结论已记入技术文档 §5。

---

## 阶段 2：播放列表与快捷键（M2，FR-07、FR-09~15、PRD §6）

### 步骤

- [x] **2.1** `Playlist` 模型：`Track`/`PlaylistState` 结构；按路径去重（大小写不敏感），非 `.mp3` 拒绝并轻提示（FR-13、FR-14）
- [x] **2.2** 列表 UI：条目显示文件名（去扩展名）与时长、当前曲目高亮、单击播放、空状态引导文案（FR-10、FR-12、FR-15）
- [x] **2.3** 文件接入：⌘O 对话框改多选入列表；拖拽入窗口（`webUtils.getPathForFile`）；工具栏「添加/删除/清空」（FR-09、FR-10）
- [x] **2.4** 上一首/下一首逻辑与边界（首尾行为按当前循环模式）；⌘←/⌘→ 对应
- [x] **2.5** 播放结束行为 + 三档循环模式切换（列表循环 → 单曲循环 → 顺序播放，默认列表循环）（FR-07、FR-11）
- [x] **2.6** 时长异步串行探测（隐藏 `<audio preload="metadata">`），失败标记不可播放（FR-12）
- [x] **2.7** 快捷键管理器：PRD §6 完整映射表（空格/方向键/⌘方向键/空格与 M、L/⌘O/⌘,）、命中 `preventDefault`、输入框焦点豁免规则
- [x] **2.8** 播放错误处理：损坏/无法解码 → 提示并自动跳下一首（FR-08）
- [x] **2.9** Vitest 单元测试：循环模式下一首计算、快捷键组合解析、扩展名过滤与去重

### 验证（对照 PRD §8 验收标准逐项）

- [x] 拖入多个 MP3 全部入列表并可连续播放（验收 1）（2026-08-27 用户确认）
- [x] ⌘←/⌘→ 切歌；三种循环模式行为正确（验收 4）（2026-08-27 用户确认）
- [x] 空格暂停/恢复在列表场景下仍正确（验收 2 复测）（2026-08-27 用户确认）
- [x] 拖入非 MP3 有轻提示且不崩溃（验收 8 前半）（2026-08-27 用户确认）
- [x] 全部快捷键逐一手动过一遍，含输入框焦点场景（2026-08-27 用户确认；⌘, 当前为占位提示，设置弹窗在阶段 3 实现）
- [x] `npm run test` 与 `npm run typecheck` 通过（22 tests passed，2026-08-27）
- [x] 提交入库，commit message 注明对应条目（2026-08-27）

---

## 阶段 3：设置与记忆（M3，FR-16~24）

### 步骤

- [x] **3.1** `electron-store` 接入主进程，按技术文档 §3.4 schema 初始化默认值（`seekStep: 5` 等）
- [x] **3.2** 设置弹窗：菜单「设置…」+ ⌘, 打开；步长输入校验 1–120 秒，修改立即生效并持久化；替换阶段 1 的步长常量（FR-16、FR-17、FR-18）
- [x] **3.3** 状态落盘：退出前完整快照、播放中约每 5 秒定期落盘进度、设置/音量/静音/速度/循环变更即时落盘（FR-19、FR-22）
- [x] **3.4** 启动恢复：加载快照 → 主进程 `filterExisting` 校验文件存在性 → 失效条目移除并轻提示 → 恢复列表/进度/音量/速度/循环，**保持暂停态**（FR-19、FR-20、FR-21）
- [x] **3.5** MediaSession 接入：曲目元数据、play/pause/previoustrack/nexttrack/seekto handlers、`setPositionState` 节流更新（FR-23、FR-24）
- [x] **3.6** 媒体键真机实测（键盘 + 耳机线控、应用失焦/最小化场景）；结论：MediaSession 路径稳定，无需切换 `globalShortcut` 兜底（兜底模块保留、默认关闭，技术文档 §3.5）
- [x] **3.7** Vitest 单元测试：设置校验（越界值、非数字拒绝）

### 验证（对照 PRD §8 验收标准逐项）

- [x] 步长改 30 秒后 ←/→ 立即生效，重启后仍生效（验收 5）（2026-08-27 用户确认）
- [x] 播放中途退出 → 重新打开：列表、曲目、进度（误差 ≤ 5 秒）、音量、速度、循环全部恢复，且为暂停态（验收 6）（2026-08-27 用户确认）
- [x] 强制杀掉进程（模拟崩溃）后重启，进度大致恢复（误差 ≤ 5 秒）（2026-08-27 用户确认）
- [x] 恢复时把某文件移走再启动：该条被移除并有提示，其余正常（验收 6 延伸）（2026-08-27 用户确认）
- [x] 应用最小化/失焦时媒体键可控；控制中心「正在播放」显示曲目标题（验收 7）（2026-08-27 用户确认）
- [x] `npm run test` 与 `npm run typecheck` 通过（26 tests passed，2026-08-27）
- [x] 提交入库，commit message 注明对应条目（2026-08-27）

> 过程记录：验证中发现「恢复后播放按钮置灰」——根因是按钮启用状态只跟随播放/暂停事件刷新，而恢复加载仅触发 `loadedmetadata`；已在按钮状态订阅中补 `loadedmetadata`，并让 `Player.unload` 发出 `statechange`，复测通过。

---

## 阶段 4：打磨与发布（M4）

### 步骤

- [x] **4.1** 错误路径全量回归：非 MP3、损坏文件、加载失败、时长探测失败的提示与降级行为统一（FR-08、FR-14、FR-21）
- [x] **4.2** 深色/浅色模式适配验证（跟随系统）
- [x] **4.3** 窗口布局回归：最小尺寸 480×360、拉大后布局不破、工具栏/列表/控制栏比例合理
- [x] **4.4** Toast 提示一致性与自动消失行为复查（PRD §5.3）
- [x] **4.5** `electron-builder` 打包 dmg + zip（arm64 + x64），安装后启动验证
- [x] **4.6** 按 PRD §8 验收标准全量回归，逐项勾选
- [x] **4.7** 文档自检（AGENTS.md §3 清单）：PRD/技术文档/README 与实现双向一致；README「当前状态」更新为已发布状态

### 验证

- [x] PRD §8 验收标准 8 项全部 ✅（2026-08-27 用户在打包产物上确认）
- [x] 打包产物在本机安装、启动、播放、退出恢复全流程通过（2026-08-27 用户确认）
- [x] 文档自检清单逐项通过（2026-08-27）
- [x] 打 tag（v1.0）并归档（2026-08-27）

> 过程记录：首次打包卡在 `codesign`——electron-builder 自动发现钥匙串中的开发者证书并等待授权；按技术文档 §6「v1.0 自用不签名」，已在 `dist` 脚本固化 `CSC_IDENTITY_AUTO_DISCOVERY=false`，重跑成功（产物未签名，首次打开右键→打开）。

---

## 阶段 5：转录文稿（v1.1，M5，FR-25~31）

> 前置：PRD §4.6 / §5.4 / §8.2 与技术文档 §3.8 的设计评审通过。真实密钥与真实英文播客音频的验证项按 AGENTS.md §6 移交用户操作。

### 步骤

- [x] **5.1** `src/shared/transcript-utils.ts`：`TranscriptSegment`/`TranscriptResult` 类型，词→句聚合与缓存键纯函数 + Vitest 单测（标点切分、停顿切分、空输入）（2026-08-28，85b744e）
- [x] **5.2** 密钥管理：`safeStorage` 加解密与 `secrets.deepgramApiKey` 落盘；设置弹窗新增密钥区（填入/替换/清除，掩码显示）；对应三个密钥 IPC（2026-08-28，e6ed213）
- [x] **5.3** `src/main/transcript.ts`（一）：Deepgram 请求与解析（读文件 → POST → words → 聚合），5 分钟超时，HTTP 状态 → 错误码映射（2026-08-28，3bc96d6）
- [x] **5.4** `src/main/transcript.ts`（二）：`userData/transcripts/` 缓存读写（路径+大小+mtime），`force` 重新转录覆盖（2026-08-28，a0a7610）
- [x] **5.5** `getTranscript` IPC 与编排：缓存 → 密钥 → 请求；代际标记丢弃切歌后的过期结果；同文件进行中请求复用（2026-08-28，a0a7610）
- [x] **5.6** 布局重构：左右分栏（左列表 / 右文稿），提高窗口最小尺寸，深色/浅色与窗口缩放回归，v1.0 功能不回归破坏（2026-08-28，ced4e91）
- [x] **5.7** 文稿面板 `ui/transcript-view.ts`：状态机（无曲目 / 未配置密钥 / 转录中 / 错误 / 就绪）、句子渲染、点击跳转、「重新转录」入口（2026-08-28，91673e5）
- [x] **5.8** 播放联动：开始播放（含自动切歌）触发转录；随 `timeupdate` 高亮当前句子并滚动到可见区域（2026-08-28，91673e5）
- [x] **5.9** 错误与降级路径：未配置引导（可跳转设置）、401/429/断网提示、重试；全程不阻塞播放（2026-08-28，7730d1e）
- [x] **5.10** 文档自检（AGENTS.md §3）+ 按 PRD §8.2 全量验收 + 打包后复验 + 版本号与发布（2026-08-28：验收通过、打包复验通过，发布提交 e69ee3f，tag v1.1）

### 验证

- [x] `npm run test` / `npm run typecheck` 通过（新增聚合函数与缓存键单测，35 tests passed，2026-08-28）
- [x] 真实密钥 + 真实英文播客，逐项通过 PRD §8.2（2026-08-28 用户确认：全部验证通过）
- [x] 布局回归：最小尺寸、拉大缩小、深色/浅色（2026-08-28 用户确认）
- [x] 提交入库，commit message 注明对应条目（发布提交 e69ee3f「实现 FR-25~31」，tag v1.1）

---

## 阶段 6：媒体库与列表状态（v1.2，M6，FR-32~36）

> 前置：PRD §4.7 / §5.5 / §8.3 与技术文档 §3.9 的设计评审通过。导入/重命名/删除等文件操作在主进程完成，渲染层不直接触碰文件系统。

### 步骤

- [x] **6.1** `src/shared/library-utils.ts`：重名冲突后缀、重命名校验（空名/分隔符/补扩展名）、已播放阈值判定（≥98%）纯函数 + Vitest 单测；`PersistedTrack`/`ImportResult` 类型（渲染层 `Track` 字段扩展随 6.3/6.4 接入）（2026-08-28）
- [x] **6.2** `src/main/library.ts`：专属库目录管理、导入拷贝（含冲突后缀）、重命名、删除（均做库目录前缀校验）+ 三个媒体库 IPC（2026-08-28）
- [x] **6.3** 渲染层导入流程改造：添加/拖拽 → `importToLibrary` → 白名单登记 → 入列表（生成稳定 `id`、`importedFrom` 去重、`addedAt`）；导入提示与失败处理（2026-08-28）
- [x] **6.4** 持久化 schema v2：`playlist` 改对象数组、`version` 字段与升级清空（含旧转录缓存）；每首 `position`/`played` 落盘与恢复（2026-08-28）
- [x] **6.5** 列表状态：每首进度续播（切歌保存/恢复）、已播放判定（`ended` 或 ≥98%）与置灰样式（2026-08-28）
- [x] **6.6** 重命名交互：双击行内编辑（回车确认 / Esc 取消）、校验与错误提示；重命名后状态与文稿关联保留（持久化文稿关联由 6.7 的曲目 id 缓存键保证）（2026-08-28）
- [x] **6.7** 转录缓存切换为曲目 `id` 键；删除条目时同步删除缓存与库内副本（2026-08-28）
- [x] **6.8** 文档自检（AGENTS.md §3）+ 按 PRD §8.3 全量验收 + 打包后复验 + 版本号与发布（1.2.0）（2026-08-28：验收通过、打包复验通过，发布提交 61b34a4）

### 验证

- [x] `npm run test` / `npm run typecheck` 通过（44 tests，2026-08-28）
- [x] 按 PRD §8.3 九项逐一验收（导入/去重/重命名/续播/已播放/删除/升级清空）（2026-08-28 用户验证全部通过）
- [x] 布局回归：左右分栏不破，深色/浅色正常（2026-08-28 验收过程中确认）
- [x] 提交入库，commit message 注明对应条目（步骤提交 e9eadb0~64afda4 均注明 FR 编号，发布提交 61b34a4「实现 FR-32~36」）

---

## 阶段 7：交互改进（v1.3，M7，FR-37~40）

> 前置：PRD §3.5 / §4.8 / §5.6 / §8.4 与技术文档 §3.10 的设计评审通过。本阶段只调整交互行为，不新增持久化字段、不改已有数据结构。

### 步骤

- [x] **7.1** 手动播放改造（FR-37）：⌘O/拖拽导入不再自动起播；单击条目 = 选中并加载（保持暂停）；切歌（单击、⌘←/⌘→、媒体键上一首/下一首）保持播放状态（播放中 → 加载新曲目续播，暂停 → 保持暂停）；起播仅剩播放按钮与空格；播放结束自动切歌不受影响（2026-08-28）
- [x] **7.2** 条目右键菜单（FR-38）：主进程 `openTrackMenu`（库目录前缀校验 + 原生 Menu + `shell.showItemInFolder`）与 `track-menu:command` 命令通道；渲染层接入 `contextmenu`（重命名/删除复用现有流程，收命令时校验界内与条目 `id`）；移除工具栏删除按钮（2026-08-28）
- [x] **7.3** 清空二次确认（FR-39）：通用 `confirmAction` 确认桥（`dialog.showMessageBox`，默认按钮为取消）+ 清空流程前置调用，文案明示库内副本将被删除（2026-08-28）
- [x] **7.4** 文稿可选择（FR-40）：句子片段 `user-select: text` + 点击跳转处理器的 `getSelection().isCollapsed` 保护（2026-08-28）
- [x] **7.5** 文档自检（AGENTS.md §3）+ 按 PRD §8.4 全量验收 + 打包后复验 + 版本号与发布（1.3.0）（2026-08-28：验收通过、打包复验通过，发布提交 f78f65b，tag v1.3）

### 验证

- [x] `npm run test` / `npm run typecheck` 通过（44 tests，步骤 7.1~7.4 每步复验，2026-08-28）
- [x] 按 PRD §8.4 七项逐一验收（手动播放/右键菜单/清空确认/文稿选择）（2026-08-28 用户验证全部通过）
- [x] 回归：既有播放、快捷键、转录、媒体库行为不变；布局与深色/浅色模式不破（2026-08-28 验收中确认）
- [x] 提交入库，commit message 注明对应条目（步骤提交 871933a~47f37ac 均注明 FR 编号，发布提交 f78f65b「实现 FR-37~40」）

---

## 阶段 8：v1.3.1 补丁（M8，FR-41、FR-42）

> 前置：PRD §4.9 / §8.5 与技术文档 §3.8、§3.10、§6 的设计评审通过。本阶段只做两处小改动：复用 v1.3 已有确认桥，不新增 IPC 通道与持久化字段。

### 步骤

- [x] **8.1** 重新转录二次确认（FR-41）：渲染层 `onRetranscribe` 先调 `confirmAction`（标题/按钮「重新转录」，详情说明消耗配额与覆盖缓存，默认按钮为取消），确认后才 `ensureTranscript(force)`（2026-08-28）
- [x] **8.2** 打包产物清理（FR-42）：`package.json` 增加 `predist` 脚本（`rm -rf release`），`npm run dist` 自动先清空产物目录再打包（2026-08-28）
- [ ] **8.3** 文档自检（AGENTS.md §3）+ 按 PRD §8.5 验收 + 版本号升至 1.3.1 与发布（含打包后复验、打 tag）

### 验证

- [ ] 按 PRD §8.5 两项逐一验收（重新转录确认的取消/确认路径；`npm run dist` 后 `release/` 仅含当前版本产物）
- [ ] `npm run test` / `npm run typecheck` 通过
- [ ] 回归：转录自动触发、缓存命中、清空确认不受影响（验收中确认）
- [ ] 提交入库，commit message 注明对应条目

---

## 附：需求 → 阶段对照表

| 需求条目 | 阶段 |
| --- | --- |
| FR-01~06 | 阶段 1 |
| FR-07 | 阶段 2（依赖循环模式） |
| FR-08 | 阶段 2 实现，阶段 4 回归 |
| FR-09~15 | 阶段 2 |
| FR-16~18 | 阶段 3 |
| FR-19~22 | 阶段 3 |
| FR-23~24 | 阶段 3 |
| FR-25~31（v1.1） | 阶段 5 |
| FR-32~36（v1.2） | 阶段 6 |
| FR-37~40（v1.3） | 阶段 7 |
| FR-41、FR-42（v1.3.1） | 阶段 8 |
| PRD §6 快捷键表 | 阶段 2（音量/步长的数值逻辑阶段 1 已备好） |
| PRD §7 非功能需求 | 阶段 4 统一验证（性能、兼容性随阶段验证覆盖） |
