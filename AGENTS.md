# AI 项目协作规范

本文档约束所有参与本项目的 AI 工具与人类协作者的行为。**核心原则：文档是唯一事实来源，任何需求修改必须先改文档、再改代码，文档与实现必须严格一致——不能多，不能少。**

## 1. 文档体系

| 文档 | 职责 | 谁维护 |
| --- | --- | --- |
| `docs/PRD.md` | 产品需求：做什么、不做什么、验收标准 | 需求变更时同步更新 |
| `docs/TECHNICAL_DESIGN.md` | 技术方案：怎么做、架构、接口、目录结构 | 方案变更时同步更新 |
| `docs/PROGRESS.md` | 开发进度：分阶段步骤清单，每步附验证项 | 每步完成/验证后更新状态 |
| `README.md` | 对外概览：功能、快捷键、技术栈、开发命令 | 功能/命令变化时同步更新 |
| `AGENTS.md` | 本协作规范 | 规范变更时更新 |

## 2. 需求修改流程（强制）

任何需求修改——无论来自用户口头、对话中的临时决定、还是实现中发现的调整——都必须走以下流程：

1. **先改文档**：在 `docs/PRD.md`（需求层面）或 `docs/TECHNICAL_DESIGN.md`（技术层面）中更新对应条目；新增需求必须有编号（如 FR-25），不允许只存在于对话记录或代码里。
2. **再改代码**：实现与文档条目一一对应。
3. **提交说明**：commit message 中注明对应的文档条目编号（如 `实现 FR-25` / `按 PRD §6 调整`）。

**禁止**：未经文档记录直接实现新功能、修改既有行为或删除已有功能。如果用户要求跳过文档，先提醒本规范，用户明确坚持时照做但在回复中指出偏差。

## 3. 文档与实现一致性（强制）

文档和实现必须双向一致：

- **不能多**：文档中不得描述未实现的功能（规划中的内容必须明确标注版本，如「v1.1 候选」「计划中」，不得写成现状）。
- **不能少**：已实现的功能、行为、快捷键、配置项必须在文档中有对应描述。
- **不能偏**：文档描述的行为细节（默认值、步长、边界条件、交互方式）必须与代码实际行为一致。

### 完成前自检清单

每次完成任务、报告"已完成"之前，必须逐项核对：

- [ ] 本次改动涉及的每条需求，在 `PRD.md` 中都有对应条目且描述与实现一致
- [ ] 技术方案有变化时，`TECHNICAL_DESIGN.md` 已同步（接口签名、数据结构、目录结构、安全配置）
- [ ] 用户可见的变化（功能、快捷键、命令、运行要求）已同步到 `README.md`
- [ ] 文档中没有因本次改动而过时的描述（搜索相关关键词复查）
- [ ] 删除的功能已从文档中移除，不留"已废弃"残影

任何一项不满足，先修文档再报告完成。

## 4. 冲突处理

- **文档与代码冲突**：以文档为准修复代码；若判断文档本身过时，先与用户确认，更新文档后再动代码。
- **文档之间冲突**（如 PRD 与技术设计不一致）：暂停实现，向用户指出冲突点，确认后再统一修订。
- **对话决定与文档冲突**：对话中的新决定覆盖旧文档，但必须立即回写文档，不允许只停留在对话里。

## 5. 范围控制

- 严格按 `PRD.md` §3.2「明确不做」控制边界：超出 v1.0 范围的功能（歌词、流媒体、曲库、跨平台、MP3 以外格式）推到后续版本，不主动实现。
- 不擅自添加文档未要求的功能、配置项、抽象层。

## 6. 修订本规范

本规范的修改需用户明确同意，修改时在文末追加变更记录。

---

变更记录：

| 日期 | 变更 |
| --- | --- |
| 2026-08-27 | 初版：确立文档先行、双向一致原则 |
| 2026-08-27 | §1 文档体系新增 `docs/PROGRESS.md`（开发进度与验证记录） |
